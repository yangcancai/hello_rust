mod lib;
use lib::functions;
use lib::data_type;
use lib::control_flow;
use lib::struction;
use lib::{image,enum_struct, oop};
fn main() {
    // guess_game::execute();
     data_type::execute();
     functions::execute();
     control_flow::execute();
     struction::execute();
     image::execute();
     enum_struct::execute();
     oop::execute();
}